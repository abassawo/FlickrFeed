<!-- A DrawerLayout is intended to be used as the top-level content view using match_parent for both width and height to consume the full space available. -->
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:id="@+id/drawer_layout"
    android:layout_width="match_parent" android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- As the main content view, the view below consumes the entire
         space available using match_parent in both dimensions. -->
    <FrameLayout
        android:id="@+id/content_frame"
        android:layout_width="match_parent"
        android:layout_height="match_parent">


        <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
            android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
            android:paddingRight="@dimen/activity_horizontal_margin"
             android:paddingTop="@dimen/activity_vertical_margin"
            android:paddingBottom="@dimen/activity_vertical_margin"
            tools:context="abassawo.c4q.nyc.FlickrFeed.MainActivity$PlaceholderFragment"
            android:background="#d51d283a"
            android:clickable="true"
            android:focusableInTouchMode="true"
            android:id="@+id/layoutHoroscope">



            <Button
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="reload"
                android:typeface="serif"
                android:id="@+id/button_reload" />

            <GridView
                android:layout_width="match_parent"
                android:layout_height="450dp"
                android:id="@+id/gridView"
                android:numColumns="auto_fit"
                android:layout_gravity="center_horizontal"
                android:layout_below="@+id/button_reload"
                android:layout_above="@+id/button_upload" />

            <Button
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:id="@+id/button_upload"
                android:text="upload"
                android:focusableInTouchMode="false"
                android:typeface="serif"
                android:layout_alignParentBottom="true"
                android:layout_alignParentRight="true"
                android:layout_alignParentEnd="true" />


    </RelativeLayout>
</FrameLayout>

    <ListView
        android:layout_width="200dp"
        android:layout_height="match_parent"
        android:id="@+id/navList"
        android:layout_gravity="left|start"
        android:background="#ff7bacff"
        android:choiceMode="singleChoice"
        android:dividerHeight="3dp"
        android:divider="@color/bright_foreground_material_dark"
        android:drawSelectorOnTop="true"
        android:clickable="true" />
</android.support.v4.widget.DrawerLayout>
